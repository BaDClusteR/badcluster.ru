<?php
/** @noinspection PhpFullyQualifiedNameUsageInspection */
/** @var \BC\Widget\Blocks $this */

$blocks = $this->getBlocks();
?>
<?php foreach ($blocks as $block) : ?>
    <?=$this->renderBlock(
        (string) ($block['type'] ?? ''),
        (array) ($block['data'] ?? '')
    )?>
<?php endforeach; ?>
